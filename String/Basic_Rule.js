const goodStr = 'Jake asks Finn, "Hey, let\'s go on an adventure?"'; 
console.log(goodStr);
//Here backslash will remove the error a wew are using single and double quotes for backslash is used so compiler can't be confused
const badStr = 'Finn responds, "Let's go!"';
console.log(badStr);